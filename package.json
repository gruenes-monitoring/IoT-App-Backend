{
  "name": "IoT-App-MQTT-Client",
  "description": "Simple Travis-CI check for JSHint (Code Linting)",
  "author": "Jonas Raddatz",
  "version": "0.1.0",
  "devDependencies": {
    "chai": "^4.1.2",
    "jshint": "^2.9.5",
    "mocha": "^5.2.0"
  },
  "scripts": {
    "test": "jshint app.js && jshint addTwoNumbers.js && jshint mqtt_client/graphql_interface.js && jshint mqtt_client/device.js && mocha",
    "start": "node app.js"
  },
  "dependencies": {
    "apollo-fetch": "^0.7.0",
    "mqtt": "^2.18.8"
  }
}
